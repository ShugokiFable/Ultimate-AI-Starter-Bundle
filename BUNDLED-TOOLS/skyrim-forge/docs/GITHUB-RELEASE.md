# GitHub release

Push the contents of the extracted `Skyrim-Forge-5.0.0` directory to the repository root. Wait for CI and CodeQL. Create tag `v5.0.0` only after all required checks pass.

The release workflow builds the deterministic repository ZIP, Python wheel, source distribution, checksums, validation report, and SBOM.
