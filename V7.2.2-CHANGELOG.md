# V7.2.2 CHANGELOG — MCP cold-start fixes + dashboard truth

2026-08-15

## What happened in the field

- Grok hung at session start: skyrim-forge MCP (dead venv) and firecrawl-mcp
  (unpinned `npx` cold-resolve exceeded the 30s startup timeout) both reported
  unavailable. Forge venv rebuilt from a stable interpreter; firecrawl pinned.
- codebase-memory v0.10.5 UI: Projects-tab NODES/EDGES tiles read 0 because the
  HTTP UI RPC allowlist rejects `get_graph_schema` (only `list_projects` passes).
  Agent MCP access is unaffected. Upstream issue filed: #1663.

## Pack changes

- `TOOLS/MCP-CONFIG-EXAMPLES.toml.txt`: firecrawl example now pins
  `firecrawl-mcp@3.24.0` with `startup_timeout_sec = 120` and explains why
  (cold npx resolve > default 30s timeout kills the server silently).
- `codebase-memory` skill: dashboard section now documents the v0.10.5 UI-RPC
  lockdown (403 on `get_graph_schema`, issue #1663) and the correct
  "verify via list_projects" path. "0 tiles" stays cosmetic, not data loss.
- Both propagated to canonical + all 10 provider trees.

## Local-only actions (documented, not pack files)

- `~/.claude.json` + `~/.grok/config.toml` + `~/.codex/config.toml` +
  `~/.kimi-code/mcp.json` + Hermes `config.yaml`: firecrawl pinned @3.24.0.
- `~/.grok/config.toml`: explicit `[mcp_servers.skyrim-forge]` +
  `[mcp_servers.firecrawl-mcp]` (user scope beats the claude.json compat import).
- Recreated `Start-codebase-memory-UI.bat` keep-alive host.
