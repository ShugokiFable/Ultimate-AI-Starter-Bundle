# Example MCP snippets for V5 tools
# Copy only what you installed. Replace paths. Restart the AI app after edits.

# ---------- Grok Build: %USERPROFILE%\.grok\config.toml ----------

# [mcp_servers.codebase-memory-mcp]
# command = "C:/Users/YOU/AppData/Local/Programs/codebase-memory-mcp/codebase-memory-mcp.exe"
# args = []
# enabled = true
# startup_timeout_sec = 90
# tool_timeout_sec = 6000

# [mcp_servers.firecrawl-mcp]
# command = "npx"
# args = ["-y", "firecrawl-mcp@3.24.0"]   # PIN IT: unpinned npx cold-resolves the latest
# enabled = true                            # package; a cold start can exceed the 30s
# startup_timeout_sec = 120                 # startup timeout and the server reports
#                                           # "unavailable" having printed nothing.

# [mcp_servers.github]
# command = "npx"
# args = ["-y", "@modelcontextprotocol/server-github@2025.4.8"]   # PIN IT (same rule)
# enabled = true

# Rule: PIN EVERY npx MCP server. Versioned pins that have been field-verified:
#   firecrawl-mcp@3.24.0  server-github@2025.4.8  @upstash/context7-mcp@4.0.2
#   @modelcontextprotocol/server-sequential-thinking@2026.7.4
# Grok-specific notes: see GROK-MCP-TROUBLESHOOTING.md. As of v7.4.0 Grok MCP is
# OFF by default ([compat.claude] mcps = false): grok-cli 1.0.4 charged ~65s to
# the first turn of every session and attached ZERO tools (tool_count stayed at
# 26, its built-in count, with 8 servers configured and with 0). Native
# [mcp_servers.*] sections DO work and persist - the older "grok rewrites them"
# note was wrong. Wire headroom through the PYTHONPATH-stripping launcher.

# [mcp_servers.housecarl]
# command = "C:/PATH/TO/housecarl-mcp.exe"
# args = []
# enabled = true
# startup_timeout_sec = 120
# tool_timeout_sec = 6000

# [mcp_servers.skyrim-forge]
# command = "C:/PATH/TO/Skyrim-Forge/.venv/Scripts/python.exe"
# args = ["-m", "skyrim_forge", "mcp"]
# enabled = true
# startup_timeout_sec = 120

# [mcp_servers.headroom]
# command = "headroom"
# args = ["mcp"]
# enabled = true

# ---------- Claude Code ----------
# Prefer official installers/plugins:
#   houseCARL-Setup.exe
#   claude plugin install for Superpowers / Ponytail / Headroom when available
#   headroom mcp install

# ---------- Codex ----------
# Use Codex MCP settings UI or config.toml per current Codex docs.
# houseCARL ships a codex\ skill stub in its zip — MCP binary still required.

# ---------- Env vars (optional, any shell profile) ----------
# setx SKYRIM_FORGE_ROOT "D:\Tools\Skyrim-Forge-x.y.z"
# setx HOUSECARL_MCP "D:\Tools\houseCARL\housecarl\server\housecarl-mcp.exe"
# setx SPOOKY_AUTOMOD_ROOT "D:\Tools\spookys-automod-toolkit"
# setx CODEBASE_MEMORY_MCP "%LOCALAPPDATA%\Programs\codebase-memory-mcp\codebase-memory-mcp.exe"
# setx SKYRIM_MO2_INSTANCE "D:\MO2\instances\MyList"
