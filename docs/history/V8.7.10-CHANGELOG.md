# v8.7.10 -- same-day shadcn convergence

**2026-09-02.** Based on v8.7.9. **167 canonical skills**; no new always-on MCP schemas.

The operator-run freshness gate caught shadcn 4.20.1 after v8.7.9 was published. The project-scoped `web` profile and component catalog now agree on that exact pin.

Both npm tarballs were compared. The MCP entrypoint changed only with rebuilt chunk names and the embedded package version. A real initialize/tools-list exchange still reports protocol 2025-06-18, 7 tools, and exactly 4,495 serialized schema bytes -- about 1,124 tokens per enabled turn.

All 19 tracked release surfaces are current. Skills, profile activation rules, Windows MCP's off-by-default state, RTK's narrow safe routing, and the three-server always-on core are unchanged.
