# v7.7.3 - portable provider settings

Starter `settings.json` / `config.toml` / `config.yaml` for every supported
provider, installed by `INSTALL-V7-AIO.ps1` without copying a live machine
and without touching `0-UNRESTRAINT-PACKS`.

Forge 5.1.5+ is the Claude Code floor. Live Forge is a versioned folder under
the Skyrim tools directory, not Documents.
