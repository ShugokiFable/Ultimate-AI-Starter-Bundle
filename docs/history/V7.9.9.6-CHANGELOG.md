# V7.9.9.6

The maintainer pasted their personal live Hermes config over the provider-tree
starter template again (third time); instead of another silent revert, the live
config is now a first-class tracked file:

- `1-TAILORED-PROVIDER-TREES/Hermes/config.MAINTAINER.yaml` - [REDACTED]'s live config,
  header-marked NOT FOR SHIPPING, excluded from release zips
  (`TOOLS/build_release.py` EXCLUDE_NAMES) and from MANIFEST
  (`TOOLS/generate_manifest.py` SKIP_NAMES).
- `config.yaml` remains the portable starter template, byte-identical to
  COPY-TO-PROVIDER-HOME per test_hermes_cost_contract.

Shipped skills: 146.
