# Don't Starve Together

Use the current game scripts as the authoritative API reference. Distinguish
client, server, and world-generation code. Validate modinfo, prefabs, components,
stategraphs, actions, recipes, assets, strings, net variables, dedicated server,
shards, configuration, and Workshop packaging.

Do not assume single-player Don't Starve APIs match DST.
